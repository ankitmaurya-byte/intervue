{
  "name": "intervue2-polling-app",
  "version": "1.0.0",
  "description": "Real-time polling app with teacher and student personas",
  "main": "server/index.js",
  "scripts": {
      "postinstall": "npx npm install --prefix server && npx npm install --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}